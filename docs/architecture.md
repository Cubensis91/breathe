# Architecture Notes (docs mirror)

See top-level [`ARCHITECTURE.md`](../ARCHITECTURE.md) for the canonical
architecture description (layers, principles, directory map).

This file exists under `docs/` per the project's documentation structure and
will hold deeper design notes (data flow diagrams, specific system designs)
as milestones progress, without duplicating the top-level overview.
